rootProject.name = "WithoutAWallpaper"

pluginManagement {
    repositories {
        gradlePluginPortal()
        maven("https://maven.minecraftforge.net/")
        maven("https://maven.parchmentmc.org")
        maven("https://repo.withoutaname.eu/gradle")
        maven("https://repo.withoutaname.eu/snapshots")
    }
}